# Secrets and environment files
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
credentials
credentials.*
secrets
secrets.*
*.secret

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.egg-info/
dist/
build/
.tox/
.nox/

# Django / local databases
*.sqlite3
*.sqlite3-journal
local_settings.py
staticfiles/
*.log
media/
# Promo feature demos are baked into the SPA image (not Django MEDIA_ROOT).
!frontend/public/media/
!frontend/public/media/**

# Docker local overrides (may contain secrets)
docker-compose.override.yml
docker-compose.*.local.yml

# Node / React
node_modules/
dist/
build/
.next/
out/
.cache/
.parcel-cache/
.turbo/

# Native apps (Expo / Electron)
apps/mobile/.expo/
apps/mobile/dist/
apps/mobile/ios/
apps/mobile/android/
apps/desktop/release/
apps/desktop/dist/
*.jks
*.mobileprovision
*.p12
*.p8
AuthKey_*.p8
*.cer
*.certSigningRequest
google-services.json
GoogleService-Info.plist
# EAS / Apple local credential material (use EAS remote credentials by default)
credentials.json
**/credentials.json
eas-credentials/
.eas/credentials/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE / editor / local tooling
.idea/
.vscode/
*.swp
*.swo
*~

# Cursor
# Shared project rules in .cursor/rules/ remain version controlled.
.cursor/*.log
.cursor/**/*.log

# Status service local SQLite
status/data/

# Temporary files, logs, and generated caches
tmp/
temp/
*.tmp
*.temp
logs/
*.pid

# Local uploaded media (runtime files; never commit customer photos)
backend/media/

# Local master/source assets (optimized production copies live elsewhere)
/original_assets/
